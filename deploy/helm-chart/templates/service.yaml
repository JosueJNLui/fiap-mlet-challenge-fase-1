apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiap-mlet.fullname" . }}
  labels:
    {{- include "fiap-mlet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fiap-mlet.selectorLabels" . | nindent 4 }}
